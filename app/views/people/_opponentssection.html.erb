<%if @person.candidates.present? %>
    <div class="row">
      <% if @person.campaign.district.at_large_district == false %>
      <%= render partial: 'shared/candidates', locals: {district: @person.campaign.district, candidates: @person.opponents} %>
      <%else%>
       <%= render partial: 'shared/candidates', locals: {district: @person.campaign.district, candidates: @person.campaign.district.jurisdiction.candidates} %>
      <%end%>
    </div>
<%end%>


