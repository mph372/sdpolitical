<p id="notice"><%= notice %></p>

<p>
  <strong>Incumbent:</strong>
</p>

<% if @report.person.blank? %>
    <%= @report.person.last_name %>
<% else %>
    <%= @report.person.title %>
<% end %>


<%= link_to 'Edit', edit_report_path(@report) %> 
<%= link_to 'Back', reports_path %>
