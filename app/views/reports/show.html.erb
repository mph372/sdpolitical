<p id="notice"><%= notice %></p>

<p>
  <strong>Incumbent:</strong>
</p>

<% if @report.incumbent.blank? %>
    <%= @report.candidate.last_name %>
<% else %>
    <%= @report.incumbent.title %>
<% end %>


<%= link_to 'Edit', edit_report_path(@report) %> 
<%= link_to 'Back', reports_path %>
